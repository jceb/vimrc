Index: vimconfig/.vim/bundle/taglist/plugin/taglist.vim
===================================================================
--- vimconfig.orig/.vim/bundle/taglist/plugin/taglist.vim	2010-05-03 08:44:03.000000000 +0200
+++ vimconfig/.vim/bundle/taglist/plugin/taglist.vim	2010-05-03 22:52:22.000000000 +0200
@@ -981,7 +987,11 @@
 " Tlist_Get_Buffer_Filetype
 " Get the filetype for the specified buffer
 function! s:Tlist_Get_Buffer_Filetype(bnum)
-    let buf_ft = getbufvar(a:bnum, '&filetype')
+    let buf_fts = split(getbufvar(a:bnum, '&filetype'), '\.')
+    if len(buf_fts) == 0
+        let buf_fts = ['']
+    endif
+    let buf_ft = buf_fts[0]
 
     if bufloaded(a:bnum)
         " For loaded buffers, the 'filetype' is already determined
